package Magic.Game;

public class Pack { // pack constructor used for drafting, will need to figure out alg for pack coalation
    public Library createPack(Library s, int i) { // pass in set (s), packs needed (i)
        while (i > 0) {

            i--;
        }
        Library pack = new Library();
        return pack;
    }
}
